<form method="post" action="{{ path('mai_edi_delete', {'id': mai_edi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mai_edi.id) }}">
    <button class="btn">Delete</button>
</form>

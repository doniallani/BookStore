{% extends 'base.html.twig' %}

{% block title %}MaiEdi index{% endblock %}

{% block body %}
    <h1>MaiEdi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mai_edi in mai_edis %}
            <tr>
                <td>{{ mai_edi.id }}</td>
                <td>{{ mai_edi.name }}</td>
                <td>
                    <a href="{{ path('mai_edi_show', {'id': mai_edi.id}) }}">show</a>
                    <a href="{{ path('mai_edi_edit', {'id': mai_edi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mai_edi_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}MaiEdi{% endblock %}

{% block body %}
    <h1>MaiEdi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mai_edi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mai_edi.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mai_edi_index') }}">back to list</a>

    <a href="{{ path('mai_edi_edit', {'id': mai_edi.id}) }}">edit</a>

    {{ include('mai_edi/_delete_form.html.twig') }}
{% endblock %}

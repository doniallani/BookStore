<form method="post" action="{{ path('review_delete', {'id': rev.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rev.id) }}">
    <button class="btnSecond">X</button>
</form>

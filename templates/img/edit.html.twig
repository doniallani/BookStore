{% extends 'base.html.twig' %}

{% block title %}Edit Img{% endblock %}

{% block body %}
    <h1>Edit Img</h1>

    {{ include('img/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('img_index') }}">back to list</a>

    {{ include('img/_delete_form.html.twig') }}
{% endblock %}
